# 🚀 完整部署指南

从零开始部署 ProjectFlow 到生产环境。

---

## 📋 部署流程概览

```
1. Supabase 配置     ← 5分钟
   ├─ 创建项目
   ├─ 执行 SQL 脚本
   └─ 配置存储桶

2. 前端配置         ← 3分钟
   ├─ 克隆代码
   ├─ 安装依赖
   └─ 配置环境变量

3. 测试验证         ← 5分钟
   └─ 功能测试

4. 生产部署（可选） ← 10分钟
   └─ Vercel/Netlify 部署
```

**总耗时**：约 15-25 分钟

---

## 步骤 1：Supabase 配置

### 1.1 创建项目

1. 访问：https://app.supabase.com
2. 点击：**New Project**
3. 填写信息：
   ```
   Name: projectflow（或任意名称）
   Database Password: 强密码（记住它）
   Region: 选择最近的区域
   Pricing Plan: Free（免费计划）
   ```
4. 点击：**Create new project**
5. 等待项目创建完成（1-2分钟）

### 1.2 执行数据库脚本

1. 项目创建完成后，点击左侧 **SQL Editor**
2. 点击：**New query**
3. 打开本地文件：`docs/sql/DATABASE_SETUP.sql`
4. **复制完整内容**（约400行）
5. 粘贴到 SQL Editor
6. 点击：**Run** 或按 `Ctrl+Enter`
7. 等待执行完成
8. 确认底部显示：**Success. No rows returned**

### 1.3 验证表创建

1. 点击左侧 **Table Editor**
2. 确认以下表存在：
   - ✅ schedules
   - ✅ schedule_updates
   - ✅ schedule_attachments
   - ✅ teams
   - ✅ team_members
   - ✅ work_groups
   - ✅ tasks
   - ✅ work_items
   - ✅ task_comments
   - ✅ team_invitations

### 1.4 创建存储桶

1. 点击左侧 **Storage**
2. 点击：**New bucket**
3. 填写：
   ```
   Name: schedule-attachments
   Public: ❌ Private（取消勾选）
   File size limit: 52428800 (50MB)
   Allowed MIME types: 留空（允许所有类型）
   ```
4. 点击：**Create bucket**

### 1.5 配置存储策略

1. 回到 **SQL Editor**
2. 新建查询
3. 复制以下 SQL：

```sql
-- 允许用户上传文件到自己的文件夹
CREATE POLICY "Users can upload files to their own folder"
ON storage.objects FOR INSERT TO authenticated
WITH CHECK (
  bucket_id = 'schedule-attachments' 
  AND (storage.foldername(name))[1] = auth.uid()::text
);

-- 允许用户查看自己的文件
CREATE POLICY "Users can view their own files"
ON storage.objects FOR SELECT TO authenticated
USING (
  bucket_id = 'schedule-attachments' 
  AND (storage.foldername(name))[1] = auth.uid()::text
);

-- 允许用户删除自己的文件
CREATE POLICY "Users can delete their own files"
ON storage.objects FOR DELETE TO authenticated
USING (
  bucket_id = 'schedule-attachments' 
  AND (storage.foldername(name))[1] = auth.uid()::text
);
```

4. 执行 SQL
5. 确认成功

### 1.6 获取 API 凭证

1. 点击左侧 **Settings** → **API**
2. 复制以下信息：
   - **Project URL**（类似：`https://xxx.supabase.co`）
   - **anon/public** key（一长串字符）

📝 **保存这两个值**，下一步需要用到！

---

## 步骤 2：前端配置

### 2.1 克隆代码

```bash
# 克隆项目
git clone <repository-url>
cd schedule-reminder

# 或者如果已下载，直接进入目录
cd schedule-reminder
```

### 2.2 安装依赖

```bash
npm install
```

等待安装完成（约 1-2 分钟）。

### 2.3 配置环境变量

1. 在项目根目录创建 `.env` 文件：

```bash
# Windows PowerShell
New-Item -Path .env -ItemType File

# 或手动创建文件
```

2. 编辑 `.env` 文件，填入刚才复制的凭证：

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

⚠️ **注意**：替换为您自己的真实值！

### 2.4 启动开发服务器

```bash
npm run dev
```

看到以下输出表示成功：

```
VITE v5.4.21  ready in XXX ms
➜ Local:   http://127.0.0.1:3000/
```

### 2.5 访问应用

在浏览器打开：`http://127.0.0.1:3000`

您应该看到登录页面！

---

## 步骤 3：功能测试

### 3.1 注册账号

1. 点击"注册"
2. 输入邮箱和密码
3. 点击"注册"按钮
4. 等待跳转（Supabase 会发送确认邮件，但可以直接登录）

### 3.2 测试个人日程

1. **创建日程**：
   - 点击"添加日程"
   - 填写：标题、启动日期、完成日期（可选）
   - 点击"添加日程"
   - 确认成功提示

2. **添加更新**：
   - 点击日程卡片的 💬 图标
   - 输入更新内容
   - 选择状态（可选）
   - 点击"选择文件"上传附件
   - 点击"提交更新"

3. **下载附件**：
   - 在历史记录中找到附件
   - 点击 ⬇️ 图标下载

4. **删除日程**：
   - 点击 🗑️ 图标
   - 确认删除

### 3.3 测试团队功能

1. **创建团队**：
   - 进入"团队概览"
   - 点击"创建团队"
   - 填写团队名称和描述
   - 提交

2. **邀请成员**：
   - 进入团队详情
   - 切换到"成员管理"
   - 输入邮箱地址
   - 发送邀请

### 3.4 测试任务管理

1. 创建任务
2. 进行 WBS 分解
3. 更新进度

---

## 步骤 4：生产部署（可选）

### 4.1 Vercel 部署

#### 准备

1. 将代码推送到 GitHub
2. 访问：https://vercel.com
3. 登录并点击：**New Project**

#### 导入项目

1. 选择您的 GitHub 仓库
2. 点击：**Import**

#### 配置

1. **Framework Preset**: Vite
2. **Build Command**: `npm run build`
3. **Output Directory**: `dist`
4. **Install Command**: `npm install`

#### 环境变量

点击 **Environment Variables**，添加：

```
VITE_SUPABASE_URL = https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY = your-anon-key-here
```

#### 部署

1. 点击：**Deploy**
2. 等待构建完成（约 2-3 分钟）
3. 访问生成的 URL

### 4.2 自定义域名（可选）

1. 在 Vercel 项目设置中点击 **Domains**
2. 添加您的域名
3. 配置 DNS 记录（按照提示）
4. 等待验证完成
5. 访问自定义域名

---

## ✅ 部署完成检查

### 核心功能

- [ ] ✅ 用户可以注册和登录
- [ ] ✅ 可以创建个人日程
- [ ] ✅ 可以添加更新记录
- [ ] ✅ 可以上传和下载附件
- [ ] ✅ 可以创建团队
- [ ] ✅ 可以邀请成员
- [ ] ✅ 可以创建任务
- [ ] ✅ 可以查看统计

### 性能检查

- [ ] ✅ 页面加载速度正常（< 3s）
- [ ] ✅ 交互响应流畅
- [ ] ✅ 无明显错误提示

### 安全检查

- [ ] ✅ 无法访问其他用户数据
- [ ] ✅ 未登录无法访问应用
- [ ] ✅ 文件上传权限正常

---

## 🐛 故障排查

### 数据库错误

**错误**: `relation "schedules" does not exist`

**解决**:
```sql
-- 重新执行数据库脚本
-- docs/sql/DATABASE_SETUP.sql
```

### 文件上传失败

**错误**: `Storage error: Bucket not found`

**解决**:
1. 确认存储桶名称：`schedule-attachments`
2. 确认存储策略已执行
3. 重新执行存储策略 SQL

### 权限错误

**错误**: `permission denied for table xxx`

**解决**:
```sql
-- 检查 RLS 策略
SELECT * FROM pg_policies 
WHERE tablename = 'schedules';

-- 确认策略存在
```

### 环境变量错误

**错误**: `Supabase URL or Key is missing`

**解决**:
1. 确认 `.env` 文件存在
2. 确认变量名正确：`VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY`
3. 重启开发服务器

---

## 📊 监控和维护

### 日常检查

- 查看 Supabase Dashboard 的 Database Health
- 监控存储空间使用（免费计划 500MB）
- 检查 Auth 用户数量
- 查看 API 请求统计

### 定期维护

**每周**:
- 检查错误日志
- 清理过期邀请

**每月**:
- 数据库备份
- 性能分析
- 用户反馈收集

### 数据备份

```sql
-- 导出重要数据
COPY (SELECT * FROM schedules) TO '/tmp/schedules_backup.csv' CSV HEADER;
COPY (SELECT * FROM teams) TO '/tmp/teams_backup.csv' CSV HEADER;
```

---

## 🔄 更新流程

### 代码更新

```bash
# 拉取最新代码
git pull origin main

# 安装新依赖
npm install

# 重启服务器
npm run dev
```

### 数据库更新

如果有数据库结构变更：

1. 查看 `docs/CHANGELOG.md`
2. 执行相应的 SQL 脚本
3. 验证数据完整性

---

## 📞 获取支持

### 文档

- 📖 [快速开始](./GETTING_STARTED.md)
- 📋 [配置检查清单](./SETUP_CHECKLIST.md)
- 🔧 [开发指南](./docs/DEVELOPMENT_GUIDE.md)

### 社区

- 💬 GitHub Discussions
- 🐛 GitHub Issues
- 📧 support@projectflow.com

---

## 🎉 部署成功！

恭喜您成功部署 ProjectFlow！

### 下一步

1. **创建您的第一个日程**
2. **邀请团队成员**
3. **开始管理项目**

### 推荐阅读

- [日程功能指南](./docs/SCHEDULE_UPDATE_GUIDE.md)
- [设计系统](./docs/DESIGN_SYSTEM.md)
- [图标系统](./docs/ICON_SYSTEM.md)

---

**部署指南版本**: 2.0  
**最后更新**: 2026-01-31
