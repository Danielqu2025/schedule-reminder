# 设计系统文档

## 🎨 Claude.ai 风格深色主题 + 新材料科技元素

本文档描述了应用的设计系统，包括色彩、排版、组件样式和科技元素融入。

---

## 一、色彩系统

### 1. 深色背景系统
```css
--bg-primary: #1a1a1a      /* 主背景 - 温暖深灰 */
--bg-secondary: #2d2d2d    /* 卡片/容器背景 */
--bg-elevated: #3a3a3a     /* 悬浮元素（输入框、按钮） */
--bg-hover: #454545        /* 悬停状态 */
```

### 2. 文字系统
```css
--text-primary: #e8e8e8    /* 主文字 - 柔和白色 */
--text-secondary: #b4b4b4  /* 次要文字 - 灰色 */
--text-muted: #8a8a8a      /* 弱化文字 - 深灰 */
```

### 3. 橙色强调系统（主要用于图标）
```css
--accent-orange: #d97757         /* 主橙色 */
--accent-orange-dark: #c96843    /* 深橙色 */
--accent-orange-light: #e89575   /* 浅橙色 */
--accent-glow: rgba(217, 119, 87, 0.15)  /* 发光效果 */
```

**使用场景：**
- ✅ Logo 图标
- ✅ 标题前的装饰符号（◆、▸、●）
- ✅ 活动导航项的图标
- ✅ 徽章（Badge）
- ✅ 卡片装饰线条
- ✅ Toast 通知图标
- ❌ 主要按钮（改用白色）

### 4. 新材料科技色系
```css
--tech-blue: #4a9eff      /* 科技蓝 */
--tech-cyan: #00d4ff      /* 科技青 */
--tech-purple: #9d6fff    /* 科技紫 */
```

**使用场景：**
- 用户头像渐变
- 进度条渐变
- 科技装饰线条
- 背景网格效果
- 团队卡片装饰

### 5. 状态色
```css
--success: #6b9f5f   /* 成功/完成 */
--warning: #d9a44e   /* 警告/待处理 */
--danger: #d46b6b    /* 危险/错误 */
--info: #5f9fb4      /* 信息 */
```

### 6. 边框系统
```css
--border-subtle: #404040   /* 细腻边框（几乎不可见） */
--border-medium: #4a4a4a   /* 中等边框（悬停状态） */
```

---

## 二、按钮系统

### 1. 主要按钮（Primary Button）- 白色风格
```css
background: #ffffff
color: #1a1a1a
font-weight: 600
```

**特点：**
- 白色背景 + 深色文字
- 悬停时：`#f0f0f0` 背景
- 轻微上移效果（-1px）
- 柔和阴影

**示例：**
- 登录/注册按钮
- 提交表单按钮
- 创建团队/任务按钮
- 主要操作按钮

### 2. 次要按钮（Secondary Button）
```css
background: var(--bg-elevated)
color: var(--text-primary)
border: 1px solid var(--border-subtle)
```

**特点：**
- 深色背景 + 白色文字
- 悬停时背景变深
- 边框增强

---

## 三、排版系统

### 1. 字体家族
```css
/* 正文 */
font-family: 'Inter', system-ui, -apple-system, sans-serif;

/* 标题 */
font-family: 'Ibarra Real Nova', serif;
```

### 2. 标题装饰
所有主要标题前都添加橙色装饰符号：

```
h1/h2::before { content: '◆'; color: var(--accent-orange); }
h3::before { content: '▸'; color: var(--accent-orange); }
列表项::before { content: '●'; color: var(--accent-orange); }
```

---

## 四、科技元素融入

### 1. 背景网格效果
```css
.material-grid-bg {
  background-image: 
    linear-gradient(rgba(74, 158, 255, 0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(74, 158, 255, 0.03) 1px, transparent 1px);
  background-size: 20px 20px;
}
```

### 2. 光晕效果
```css
.tech-glow {
  box-shadow: 0 0 20px rgba(74, 158, 255, 0.1);
}
```

### 3. 装饰线条
```css
/* 顶部橙色渐变线 */
element::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 2px;
  background: linear-gradient(90deg, var(--accent-orange), transparent);
}

/* 左侧科技蓝渐变线 */
element::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 3px;
  background: linear-gradient(180deg, var(--tech-blue), var(--tech-cyan));
}
```

### 4. 科技背景
```css
/* 登录页面/邀请页面背景 */
background-image: 
  radial-gradient(circle at 20% 30%, rgba(74, 158, 255, 0.05) 0%, transparent 50%),
  radial-gradient(circle at 80% 70%, rgba(0, 212, 255, 0.03) 0%, transparent 50%),
  linear-gradient(rgba(74, 158, 255, 0.02) 1px, transparent 1px),
  linear-gradient(90deg, rgba(74, 158, 255, 0.02) 1px, transparent 1px);
background-size: 100% 100%, 100% 100%, 30px 30px, 30px 30px;
```

### 5. 渐变元素
```css
/* Logo 图标 */
background: linear-gradient(135deg, var(--accent-orange), var(--accent-orange-light));

/* 用户头像 */
background: linear-gradient(135deg, var(--tech-blue), var(--tech-cyan));

/* 进度条 */
background: linear-gradient(90deg, var(--tech-blue), var(--tech-cyan));

/* 图表柱状图 */
background: linear-gradient(180deg, color1, color2);
```

---

## 五、新材料科技主题体现

### 1. 视觉元素
- **网格背景**：模拟材料结构的分子网格
- **渐变色**：代表材料的多层次特性
- **蓝青色调**：科技感、创新感
- **橙色点缀**：能量、活力、创新突破

### 2. 交互体验
- **流畅动画**：0.2s-0.4s 过渡
- **微妙悬停效果**：卡片上移 2px
- **柔和阴影**：符合材料科学的层次感
- **渐进式显示**：fadeIn、slideIn 动画

### 3. 信息层次
- **橙色图标**：重要信息标识
- **装饰线条**：内容区块分隔
- **渐变进度条**：视觉引导
- **徽章系统**：状态快速识别

---

## 六、组件示例

### 1. 卡片（Card）
```css
background: var(--bg-secondary);
border: 1px solid var(--border-subtle);
border-radius: var(--radius-md);
box-shadow: var(--shadow-sm);

/* 悬停效果 */
:hover {
  border-color: var(--border-medium);
  box-shadow: var(--shadow-md);
  transform: translateY(-2px);
}

/* 左侧装饰（可选） */
::before {
  content: '';
  position: absolute;
  left: 0;
  width: 3px;
  background: gradient;
}
```

### 2. 输入框（Input）
```css
background-color: var(--bg-elevated);
border: 1px solid var(--border-subtle);
color: var(--text-primary);

:focus {
  border-color: var(--text-secondary);
  box-shadow: 0 0 0 3px rgba(180, 180, 180, 0.1);
}
```

### 3. 徽章（Badge）
```css
padding: 0.25rem 0.75rem;
border-radius: 9999px;
font-size: 0.75rem;
font-weight: 600;
background: rgba(color, 0.15);
border: 1px solid rgba(color, 0.3);
```

---

## 七、响应式设计

### 断点
- **移动端**：< 768px
- **平板**：768px - 1024px
- **桌面**：> 1024px

### 适配策略
- 侧边栏在移动端折叠/固定定位
- 网格布局自动调整列数
- 表单从双列变为单列
- 字体大小适当缩小

---

## 八、可访问性

### 对比度
- 文字与背景对比度 ≥ 4.5:1
- 大字体（标题）对比度 ≥ 3:1
- 白色按钮在深色背景上清晰可见

### 交互反馈
- 所有交互元素有悬停状态
- 按钮有 active 状态
- 输入框有 focus 状态
- 禁用状态明确标识

---

## 九、设计原则

1. **简洁优先**：减少视觉噪音，突出核心内容
2. **橙色点缀**：橙色仅用于图标和小元素，不喧宾夺主
3. **科技感**：通过渐变、网格、线条体现科技创新
4. **柔和对比**：避免刺眼的纯白纯黑，使用柔和色调
5. **层次清晰**：通过阴影、边框、间距建立视觉层次
6. **一致性**：所有页面使用统一的设计语言

---

## 十、维护建议

1. **颜色变量**：所有颜色统一在 `:root` 中定义
2. **组件复用**：使用全局样式类（.btn-primary、.card）
3. **命名规范**：BEM 命名法或语义化命名
4. **模块化**：每个页面独立 CSS 文件
5. **文档更新**：新增组件时更新此文档

---

**版本：** 2.0  
**更新日期：** 2026-01-31  
**设计风格：** Claude.ai Dark Theme + 新材料科技元素
